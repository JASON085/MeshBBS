# MeshBBS b0602w 維護紀錄
- 移除節點頁的診斷記錄區與「使用廣播模式」選項。
- Android Client 補上 BBS 回應相容解析，支援 compact `t=P`、`t=R`，以及 `type=post` 搭配 compact `p`/`r` 欄位。
- 修正文章列表可顯示但點進文章會出現「找不到文章」的問題。
- PC 端 `meshtastic_bbs_server.py` 的 `LIST`、`POSTS`、`READ` 同步補上 compact `t/b/p/r/n/g` 欄位，與 Android Server 協定一致，同時保留原本 `type/boards/posts/post/replies` 欄位相容舊 Client。
- Client 版本號升至 `b0602w`。
- Client APK：`android/MeshtasticBBS/apk_archive/MeshBBS-b0602w.apk`
