# 維護紀錄 b0603h

日期：2026-05-20

## 修改內容

- 優化 Android Server 登入速度，LOGIN 回傳移除線上使用者清單，改與 PC Server 較接近。
- Android Server 熱路徑 JSON 改為緊湊格式，減少封包大小。
- 移除每次 BBS 請求後立即刷新 Dashboard，避免與 LoRa 回傳封包互搶時間。
- Server 回傳多 chunk 時改為只記錄一次摘要，減少 UI log 更新造成的額外延遲。
- 版本號更新為 `b0603h`。

## 驗證

- `python -m py_compile android/MeshtasticBBS/app/src/main/python/meshbbs_android_server.py` 通過。
- `build_apk.ps1 -Variant server -BuildType release` 成功。
- 輸出 APK：
  - `android/MeshtasticBBS/app/build/outputs/apk/server/release/MeshBBS-server-b0603h.apk`
  - `android/MeshtasticBBS/apk_archive/MeshBBS-server-b0603h.apk`
