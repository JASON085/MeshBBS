# 維護紀錄 b0603j

日期：2026-05-20

## 修改內容

- Android Server 主資料庫檔名改為 `bbs.db`，與 PC Server 對齊。
- 若手機內已有舊版 `meshbbs_android_server.db`，新版啟動時會自動搬移到 `bbs.db`。
- Android Server 備份檔改用 `bbs-時間.db` 命名，方便與 PC Server 互換。
- 匯入 DB 時增加 MeshBBS schema 檢查，避免誤匯入非 MeshBBS DB。
- 版本號更新為 `b0603j`。

## 驗證

- `python -m py_compile android/MeshtasticBBS/app/src/main/python/meshbbs_android_server.py` 通過。
- `build_apk.ps1 -Variant server -BuildType release` 成功。
- 輸出 APK：
  - `android/MeshtasticBBS/app/build/outputs/apk/server/release/MeshBBS-server-b0603j.apk`
  - `android/MeshtasticBBS/apk_archive/MeshBBS-server-b0603j.apk`
