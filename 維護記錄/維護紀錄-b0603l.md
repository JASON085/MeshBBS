# 維護紀錄 b0603l

日期：2026-05-21

## 修改內容

- Android Server 診斷記錄改為完整保留，不再被後續訊息覆蓋。
- Android Server 診斷頁新增複製到剪貼簿功能，可直接複製完整診斷記錄。
- Android Server 私訊 `?` 改為回覆 3 段使用說明，內容比照 PC Server。
- Android Server 對非 `?` 的一般 Meshtastic 私訊不再回覆 `未知指令`，避免誤對其他節點持續私訊。
- 版本號更新為 `b0603l`。

## 驗證

- `python -m py_compile android/MeshtasticBBS/app/src/main/python/meshbbs_android_server.py` 通過。
- `build_apk.ps1 -Variant server -BuildType release` 成功。
- 輸出 APK：
  - `android/MeshtasticBBS/app/build/outputs/apk/server/release/MeshBBS-server-b0603l.apk`
  - `android/MeshtasticBBS/apk_archive/MeshBBS-server-b0603l.apk`
