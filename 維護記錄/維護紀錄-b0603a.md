# MeshBBS b0603a 維護紀錄
- 從文章頁返回文章列表時會重新讀取列表，避免文章標題編輯後列表仍顯示舊標題。
- Android Client 文章詳情頁新增推薦狀態與推薦數顯示，按第二次會依 Server 回應取消推薦。
- PC 端 Server 與 Android Server 的 `POSTS` / `READ` 回應補上 `push_count` 與 `pushed`，和既有 `post_pushes` 單帳號單篇 toggle 機制一致。
- Client 版本號升至 `b0603a`。
