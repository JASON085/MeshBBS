# MeshBBS b0602y 維護紀錄
- 文章詳情讀取加入自動重試：進入文章後若未收到完整 `READ` 回應，會自動重送最多 2 次，降低 LoRa 分片遺失造成第一次進文章失敗的機率。
- 若文章重試後仍未成功，會結束載入狀態並提示使用者再試一次。
- 節點清單新增 `NODEINFO_APP` payload 解析，直接從 Meshtastic 節點資訊封包解析長名稱與短名稱。
- 常用節點若原本只存節點 ID，收到完整名稱後會自動更新常用清單名稱。
- Client 版本號升至 `b0602y`。
