# 維護記錄 b0603m

日期：2026-05-21

## 修改內容

- 回覆文章送出成功後，改為自動跳回文章頁並重新讀取文章與回覆內容。
- Android Server 私訊 `?` 改為更短的多段訊息，降低單筆封包過大導致後段遺失的機率。
- Android Server 對 plain text `?` 增加短時間去重，避免同一個 `?` 被重複收到時重覆回覆多次。
- plain text 說明訊息段落間隔拉長，更接近 PC Server 的送法。
- 版本號更新為 `b0603m`。

## 驗證

- `python -m py_compile android/MeshtasticBBS/app/src/main/python/meshbbs_android_server.py` 通過。
- `build_apk.ps1 -Variant server -BuildType release` 成功。
- 輸出 APK：
  - `android/MeshtasticBBS/app/build/outputs/apk/server/release/MeshBBS-server-b0603m.apk`
  - `android/MeshtasticBBS/apk_archive/MeshBBS-server-b0603m.apk`
