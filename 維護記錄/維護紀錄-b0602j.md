# MeshBBS b0602j 維護紀錄
- Android Server 版 APK 輸出檔名改為 `MeshBBS-server-版本號.apk`，避免和一般 MeshBBS Client APK 混拿。
- `build_apk.ps1` 已同步支援封存與回填 `MeshBBS-*.apk`、`MeshBBS-server-*.apk` 兩種命名。
- 本次版本號由 `b0602i` 升為 `b0602j`，避免與既有 APK 重複。
