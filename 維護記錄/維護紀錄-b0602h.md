# MeshBBS b0602h 維護紀錄

- 檢查 Android 顯示字串，修正登入頁、連線狀態、Toast、系統訊息中的亂碼。
- 修正 Android BBS:RES 解析錯誤訊息中的亂碼。
- 修正 PC server/client 少量 BBS chunk log 與解析錯誤文字亂碼。
- 已確認既有 APK 最高為 `MeshBBS-b0602g.apk`，本次版本號升為 `b0602h`，新版 APK 檔名預計為 `MeshBBS-b0602h.apk`。
- 驗證：亂碼特徵掃描通過；`meshtastic_bbs_server.py`、`mesh_bbs_client.py`、`build_tool.py` 通過 `py_compile`；`build_server.spec` 仍指向 `bbs_gui.py` / `MeshtasticBBS_Server`。
- APK 建置問題已修復：原因為 Avast Web/Mail Shield 代簽 Maven TLS 憑證，加上 Gradle 8.9 快取缺少多個 `metadata.bin`。已讓 `build_apk.ps1` 自動建立 `C:\Users\JASON\.gradle\meshbbs-truststore.jks` 並加入 Avast 根憑證，同時保留 Android Gradle Plugin 的 `resolutionStrategy`。
- 已清理損壞的 Gradle `kotlin-dsl`、module metadata、transforms 快取後重新建置成功。
- 已成功產生 `MeshBBS-b0602h.apk`，並保留到 `android/MeshtasticBBS/apk_archive/`。
