# MeshBBS b0602u 維護紀錄

- 節點清單改回接近舊版 MeshBBS 的排序方式：名稱含有 `BBS` 的節點優先排在最上方，其餘節點依最新抓到時間排序。
- `BBS` 節點新增明顯標示與高亮底色，方便快速辨識可用的 BBS 伺服器。
- 常用節點清單新增長按刪除，會先跳出確認視窗再移除。
- 保留 Client APK 瘦身結果，`MeshBBS-b0602u.apk` 仍維持約 10.6 MB。
- Client 版本號更新為 `b0602u`，避免與既有 APK 重複。
- 已產出新版 Client APK：`android/MeshtasticBBS/apk_archive/MeshBBS-b0602u.apk`
