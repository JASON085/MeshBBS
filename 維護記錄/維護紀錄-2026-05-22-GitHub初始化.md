# GitHub 初始化整理

日期：2026-05-22

## 發現問題

- 原本 `README.md` 有明顯亂碼，不適合直接公開。
- 專案內含不適合上傳的本機資料：
  - `bbs.db`
  - `android/MeshtasticBBS/signing/signing.properties`
  - `android/MeshtasticBBS/signing/*.jks`
  - `build/`、`dist/`、`android/MeshtasticBBS/apk_archive/` 等建置產物
- `android/MeshtasticBBS/build_apk.ps1` 原本含有硬編碼 release keystore 密碼，不適合公開。

## 本次整理

- 新增適合 Python + Android 混合專案的 `.gitignore`
- 重寫 `README.md`，補上用途、安裝方式、執行方式
- 將 Android release signing 改為讀取本機私有設定或環境變數
- 準備初始化 git repo 與首次 commit

## 備註

- `meshtastic_bbs_server.py` 與 `bbs_gui.py` 內仍有預設管理員密碼 `admin` 的邏輯，公開後建議實際使用時改為自訂密碼。
